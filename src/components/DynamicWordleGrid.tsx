import { Grid2 } from "@mui/material"

interface DynamicWordleGridProps {
    size: number,
    attempts: number
}

const DynamicWordleGrid = ({size, attempts}: DynamicWordleGridProps) => {

    const 
    

  return (
    <Grid2 container spacing={2}>

    </Grid2>
  )
}

export default DynamicWordleGrid